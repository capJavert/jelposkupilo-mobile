# Copy this file to android/keystore.properties and fill real values.
# Do not commit android/keystore.properties.

# Path to your upload keystore file.
# Use an absolute path, or a path relative to android/.
storeFile=keystore/upload-keystore.jks

# Upload keystore password.
storePassword=YOUR_STORE_PASSWORD

# Upload key alias inside the keystore.
keyAlias=YOUR_KEY_ALIAS

# Upload key password.
keyPassword=YOUR_KEY_PASSWORD
